#include <iostream>
#include <cstdlib>
#include <vector>

using namespace std;

int main()
{
    // // set inputs
    // vector<int> inputsPerLayer = {1, 1};
    // // set neurons per layer
    // vector<int> neuronsPerLayer = {2, 2, 1};

    // Net test = Net(inputsPerLayer, neuronsPerLayer, 0.05);
    // // testing net
    // vector<double> outs = test.forwardPass({1, 1});
    // cout << "outputs: ";
    // for (int i = 0; i < outs.size(); i++)
    // {
    //     cout << outs.at(i) << " ";
    // }

    cout << "Hello World!" << endl;

    return 0;
}